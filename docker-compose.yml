version: '3'
services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    ports:
      - '5000:5000'
  admin:
    build:
      context: ./apps/admin
      dockerfile: Dockerfile
    ports:
      - '4000:4000'